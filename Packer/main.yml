- name: Update apt cache
  ansible.builtin.apt:
    update_cache: true

- name: Install Python and pip
  ansible.builtin.apt:
    name:
      - python3
      - python3-pip
    state: present

- name: Install Python packages
  ansible.builtin.pip:
    name: "{{ pip_packages }}"
    executable: pip3